-- Local handoff database dump placeholder.
--
-- Before final handoff, replace this file with the raw SQL dump exported from the
-- current project database. Keep this repository private if the dump contains real
-- users, password hashes, sessions, submissions, or secrets.
--
-- Until the real dump is added, use the fallback schema + seed flow documented
-- in README.md and handoff-docs/DATABASE_AND_SQL.md.
\echo 'db-seeds/local_handoff_dump.sql is a placeholder. Replace it with the handoff dump or use the fallback schema setup.'
\quit 1
