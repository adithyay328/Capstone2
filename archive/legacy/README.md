# Legacy Archive

These files are preserved for project history only. Do not use them for normal newcomer setup.

Use the root `README.md` for local handoff setup. Use `SQL_SETUP/setupDB_Master.sql` only as the schema fallback path if the local handoff dump is unavailable or stale.

Archived here:

- `SQL_SETUP/setupAll.sql`: flattened hosted SQL-editor convenience file.
- `SQL_SETUP/seed_data.sql`: older demo seed bundle.
- `SQL_SETUP/setupDB_Lab0_IntroAddition.sql`: older duplicate lab seed.
- `schema.sql`: older/minimal schema reference.
- `database_setup.md`: older PostgreSQL setup note.
- `HOSTED_TO_LOCAL_DEV_README.md`: transitional hosted-to-local note replaced by the root README.
