# If your tooling only reads DATABASE_URL, you can set it to the same URL instead:
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

# Server-side URL for the Flask emulator/grading backend.
# Keep localhost for local dev; set this in your hosting provider for production.
BACKEND_URL=http://localhost:25565
